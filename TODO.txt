importing:
+ create a thumbnail
  - called "thumbnail" image
  - scale to 400x400px (smallest side should be 400px)
  - allow to select JPEG-quality
+ keep the original image
  - called "big" image
+ create a scaled down version
  - called "regular" image
  - scale to 50% of the original size

for each image save:
+ the default size for displaying the image, called "display" size
+ the default size for previewing the image, called "preview" size

CSS:
div {
  width: <display_width>px;
  height: <display_height>px;
}
img {
  width: 100%;
  height: 100%;
  background: url('abc.jpeg') -<xoffset>px -<yoffset>px;
  background-size: <display_width+xoffset>px <display_height+yoffset>px;
  background-repeat: no-repeat;
}
